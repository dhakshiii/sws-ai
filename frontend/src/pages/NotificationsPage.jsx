export function NotificationsPage() {
  return (
    <section className="rounded-[28px] border border-dashed border-slate-300 bg-white/80 px-6 py-10 text-center shadow-soft">
      <p className="text-sm font-semibold uppercase tracking-[0.25em] text-brand-700">
        Notifications
      </p>
      <h2 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
        Notification center placeholder
      </h2>
      <p className="mx-auto mt-3 max-w-xl text-sm leading-7 text-slate-600">
        This route is active now so we can attach realtime notifications and
        unread state in the upcoming phases without restructuring the app.
      </p>
    </section>
  );
}
